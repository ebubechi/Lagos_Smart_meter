import 'package:flutter/material.dart';
import 'package:meters/ui/constants/colors.dart';

// import 'package:meters/ui/responsive/size_config.dart';
const appbarTextStyle = const TextStyle(color: kctitleTextColor);
// final titleTextStyle = TextStyle(
//     fontSize: 0.3 * SizeConfig.textMultiplier, color: kctitleTextColor);
// final textStyle = TextStyle(
//     fontSize: 0.2 * size.width, color: kctextAndIconsColor);
const normalTextStyle =
    const TextStyle(fontSize: 13.0, color: kctextAndIconsColor);
